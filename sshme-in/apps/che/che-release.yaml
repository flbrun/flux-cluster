apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: che
spec:
  interval: 5m
  chart:
    spec:
      chart: eclipse-che
      sourceRef:
        kind: HelmRepository
        name: eclipse-che
        namespace: flux-system

